services:
  api:
    build: .
    container_name: diabetes_api
    ports:
      - "8000:8000"
    env_file:
      - .env.docker
    depends_on:
      - mongo

  mongo:
    image: mongo:7
    container_name: diabetes_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: